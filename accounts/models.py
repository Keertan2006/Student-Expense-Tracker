"""
Accounts app models.

This app handles user authentication and profile management.
Uses Django's built-in User model, so no custom models needed here.
"""

# Using Django's built-in User model
# No additional models required for basic authentication
